# Maps With IOS Map Issue Example

## 🚀 How to reproduce

- Install packages with `npm install`.
- Install the build on ios device
- Start Expo
  - `npx expo start --go`
  - open the installed app on the device
 
